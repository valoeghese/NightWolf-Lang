package tk.valoeghese;

// using core.system; || system.println
using java.lang.System;

func main = (val[] args) -> {
	"Hello, World!" -> System.out.println;
};

func other = (val a, otherargs, int x, y, z) -> {
	var n = y * z + x * z;
	x * n + z;
};