package tk.valoeghese;

using core.system;

func main = (val[] args) -> {
	"Hello, World!" -> system.println;
};

func other = (val a, otherargs, int x, y, z) -> {
	var n = y * z + x * z;
	x * n + z;
};